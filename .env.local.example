# X (Twitter) API — OAuth 1.0a User Context
# Generate at developer.x.com -> Project -> App -> Keys and tokens.
# App must have Read AND Write permissions before regenerating tokens.

X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=
